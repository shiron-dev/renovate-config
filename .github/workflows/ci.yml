on:
  pull_request:
  push:
    branches:
      - main

jobs:
  all-status-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - test-check
      - lint-check
      - renovate-dry-run
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - name: Check all-status-check
        run: |
          diff \
            <(yq ".jobs | del(.all-status-check) | keys.[]" .github/workflows/ci.yml) \
            <(yq ".jobs.all-status-check.needs.[]" .github/workflows/ci.yml)
      - name: Fail if any needed job failed
        env:
          JOBS: ${{ toJson(needs) }}
        run: |
          for result in $(jq -r '.[].result' <<<"$JOBS"); do
            if [[ ! "$result" =~ ^(success|skipped)$ ]]; then
              exit 1
            fi
          done

  test-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: shiron-dev/actions/setup-node@8780bd20d1ef046844aa1a1c2e9d96fefaa1384b # v1.7.0
      - name: Install package
        run: pnpm install --frozen-lockfile
      - name: Test
        run: pnpm test

  lint-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: shiron-dev/actions/setup-node@8780bd20d1ef046844aa1a1c2e9d96fefaa1384b # v1.7.0
      - name: Install package
        run: pnpm install --frozen-lockfile
      - name: Lint
        run: pnpm lint

  renovate-dry-run:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
      - uses: shiron-dev/actions/setup-node@8780bd20d1ef046844aa1a1c2e9d96fefaa1384b # v1.7.0
      - name: Install package
        run: pnpm install --frozen-lockfile
      - name: Dry-run Renovate
        id: renovate-dry-run
        run: |
          set +e
          RENOVATE_LOG=$(pnpm exec renovate --dry-run=full --require-config=ignored --platform=local)
          EXIT_CODE=$?
          echo "$RENOVATE_LOG"
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          {
            echo "log<<$EOF"
            echo "$RENOVATE_LOG"
            echo "$EOF"
          } >> "$GITHUB_OUTPUT"
          exit $EXIT_CODE
        env:
          LOG_LEVEL: debug
          LOG_FORMAT: json
          RENOVATE_BASE_BRANCHES: ${{ github.head_ref || 'main' }}
          RENOVATE_CONFIG_FILE: default.json
      - name: Check deps
        id: check-deps
        env:
          RENOVATE_LOG: ${{ steps.renovate-dry-run.outputs.log }}
        run: |
          DEP_OUT=$( printf '%s\n' "$RENOVATE_LOG" \
            | jq --raw-output '
              select(.msg == "packageFiles with updates") |
              .config |
              to_entries |
              group_by(.key) |
              map({
                key: .[0].key,
                files: map(.value) | flatten
              }) |
              map({
                key,
                files: .files | map({
                  packageFile,
                  deps: .deps | map({
                    depName,
                    currentValue,
                    currentDigest
                  })
                })
              })
            ' )
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          {
            echo "comment<<$EOF"
            echo "$DEP_OUT" | jq -r '
              .[] |
              "<details><summary>\(.key)</summary><blockquote>\n" +
              (.files | map(
                "<details><summary>\(.packageFile)</summary>\n\n" +
                (.deps | map(if .currentDigest then " - `\(.depName) \(.currentValue)@\(.currentDigest)`" else " - `\(.depName) \(.currentValue)`" end) | join("\n")) +
                "\n</details>"
              ) | join("\n\n") ) +
              "\n</blockquote></details>"
              '
            echo "$EOF"
          } >> "$GITHUB_OUTPUT"
          echo "$DEP_OUT" | jq -r '
            .[] | .key as $mgr |
            .files[] | .packageFile as $file |
            .deps[] |
            if .currentDigest then "\($mgr) \($file) \(.depName) \(.currentValue)@\(.currentDigest)"
            else "\($mgr) \($file) \(.depName) \(.currentValue)"
            end
          ' | sort > /tmp/pr-deps.txt
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        if: github.event_name == 'pull_request'
        with:
          ref: main
      - name: Configure pnpm policy (main)
        if: github.event_name == 'pull_request'
        run: |
          cat > pnpm-workspace.yaml <<'YAML'
          allowBuilds:
            better-sqlite3: true
            core-js-pure: false
            dtrace-provider: false
            protobufjs: false
            re2: true

          minimumReleaseAgeExclude:
            - '@shiron-dev/*'

          shellEmulator: true
          YAML
      - name: Install package (main)
        if: github.event_name == 'pull_request'
        run: pnpm install --frozen-lockfile
      - name: Dry-run Renovate (main)
        if: github.event_name == 'pull_request'
        id: renovate-dry-run-main
        run: |
          set +e
          RENOVATE_LOG=$(pnpm exec renovate --dry-run=full --require-config=ignored --platform=local)
          echo "$RENOVATE_LOG"
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          {
            echo "log<<$EOF"
            echo "$RENOVATE_LOG"
            echo "$EOF"
          } >> "$GITHUB_OUTPUT"
        env:
          LOG_LEVEL: debug
          LOG_FORMAT: json
          RENOVATE_BASE_BRANCHES: main
          RENOVATE_CONFIG_FILE: default.json
      - name: Check deps (main)
        if: github.event_name == 'pull_request'
        env:
          RENOVATE_LOG: ${{ steps.renovate-dry-run-main.outputs.log }}
        run: |
          DEP_OUT=$( printf '%s\n' "$RENOVATE_LOG" \
            | jq --raw-output '
              select(.msg == "packageFiles with updates") |
              .config |
              to_entries |
              group_by(.key) |
              map({
                key: .[0].key,
                files: map(.value) | flatten
              }) |
              map({
                key,
                files: .files | map({
                  packageFile,
                  deps: .deps | map({
                    depName,
                    currentValue,
                    currentDigest
                  })
                })
              })
            ' )
          echo "$DEP_OUT" | jq -r '
            .[] | .key as $mgr |
            .files[] | .packageFile as $file |
            .deps[] |
            if .currentDigest then "\($mgr) \($file) \(.depName) \(.currentValue)@\(.currentDigest)"
            else "\($mgr) \($file) \(.depName) \(.currentValue)"
            end
          ' | sort > /tmp/main-deps.txt
      - name: Generate diff
        if: github.event_name == 'pull_request'
        id: deps-diff
        run: |
          set +e
          DIFF=$(diff -u --label main --label pr /tmp/main-deps.txt /tmp/pr-deps.txt)
          set -e
          if [ -n "$DIFF" ]; then
            EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
            {
              echo "diff<<$EOF"
              echo "$DIFF"
              echo "$EOF"
            } >> "$GITHUB_OUTPUT"
          fi
      - name: Build comment
        if: github.event_name == 'pull_request'
        id: build-comment
        env:
          DEPS_COMMENT: ${{ steps.check-deps.outputs.comment }}
          DEPS_DIFF: ${{ steps.deps-diff.outputs.diff }}
        run: |
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          {
            echo "message<<$EOF"
            echo "## Renovate dry-run detected dependencies"
            echo "$DEPS_COMMENT"
            if [ -n "$DEPS_DIFF" ]; then
              echo ""
              echo "## Diff from main"
              echo '```diff'
              echo "$DEPS_DIFF"
              echo '```'
            fi
            echo "$EOF"
          } >> "$GITHUB_OUTPUT"
      - uses: marocchino/sticky-pull-request-comment@0ea0beb66eb9baf113663a64ec522f60e49231c0 # v3.0.4
        if: github.event_name == 'pull_request' && steps.check-deps.outputs.comment != ''
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          number: ${{ github.event.pull_request.number }}
          header: deps-check
          message: ${{ steps.build-comment.outputs.message }}
